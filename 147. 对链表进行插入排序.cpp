
#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    ListNode* insertionSortList(ListNode* head) {
        if(!head) return head;

        auto next = head->next;
        head->next = nullptr;
        while(next){

        }
    }
};
int main() {

    return 0;
}
